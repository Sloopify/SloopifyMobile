import 'package:flutter/material.dart';

class FilterEffectsImage extends StatelessWidget {
  final int index;
  const FilterEffectsImage({super.key,required this.index});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
